<Page
    x:Class="agenzia2.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:behaviors="using:agenzia2.Behaviors"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    xmlns:helpers="using:agenzia2.Helpers"
    xmlns:views="using:agenzia2.Views"
    Loaded="OnLoaded"
    mc:Ignorable="d">

    <winui:NavigationView
        x:Name="navigationView"
        IsBackButtonVisible="Visible"
        IsBackEnabled="{x:Bind IsBackEnabled, Mode=OneWay}"
        SelectedItem="{x:Bind Selected, Mode=OneWay}"
        ItemInvoked="OnItemInvoked"
        IsSettingsVisible="True"
        Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
        <winui:NavigationView.MenuItems>
            <!--
            TODO WTS: Change the symbols for each item as appropriate for your app
            More on Segoe UI Symbol icons: https://docs.microsoft.com/windows/uwp/style/segoe-ui-symbol-font
            Or to use an IconElement instead of a Symbol see https://github.com/Microsoft/WindowsTemplateStudio/blob/master/docs/projectTypes/navigationpane.md
            Edit String/en-US/Resources.resw: Add a menu item title for each page
            -->
            <winui:NavigationViewItem x:Uid="Shell_Auto" Icon="Document" helpers:NavHelper.NavigateTo="views:AutoPage" />
            <winui:NavigationViewItem x:Uid="Shell_Moto" Icon="Document" helpers:NavHelper.NavigateTo="views:MotoPage" />
            <winui:NavigationViewItem x:Uid="Shell_Speciale" Icon="Document" helpers:NavHelper.NavigateTo="views:SpecialePage" />
            <winui:NavigationViewItem x:Uid="Shell_Camion" Icon="Document" helpers:NavHelper.NavigateTo="views:CamionPage" />
            <winui:NavigationViewItem x:Uid="Shell_Pra" Icon="Document" helpers:NavHelper.NavigateTo="views:PraPage" />
            <winui:NavigationViewItem x:Uid="Shell_Mtc" Icon="Document" helpers:NavHelper.NavigateTo="views:MtcPage" />
            <winui:NavigationViewItem x:Uid="Shell_Patenti" Icon="Document" helpers:NavHelper.NavigateTo="views:PatentiPage" />
            <winui:NavigationViewItem x:Uid="Shell_Ciclo" Icon="Document" helpers:NavHelper.NavigateTo="views:CicloPage" />
        </winui:NavigationView.MenuItems>
        <i:Interaction.Behaviors>
            <behaviors:NavigationViewHeaderBehavior
                DefaultHeader="{x:Bind Selected.Content, Mode=OneWay}">
                <behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
                    <DataTemplate>
                        <Grid>
                            <TextBlock
                                Text="{Binding}"
                                Style="{ThemeResource TitleTextBlockStyle}"
                                Margin="{StaticResource SmallLeftRightMargin}" />
                        </Grid>
                    </DataTemplate>
                </behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
            </behaviors:NavigationViewHeaderBehavior>
        </i:Interaction.Behaviors>
        <Grid>
            <Frame x:Name="shellFrame" />
        </Grid>
    </winui:NavigationView>
</Page>
